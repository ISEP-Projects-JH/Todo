pipeline {
    agent any

    stages {
        stage('Initialize') {
            steps {
                echo 'Jenkins pipeline initialized.'
            }
        }
        // Add future test stages here
    }
}
